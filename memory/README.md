# Memory

这里存放机器人的长期记忆。

建议记录：

- 用户偏好
- 项目约定
- 关键决策
- 常见故障与修复经验
- 长期待办

建议写法：

```markdown
## 2026-04-02
- 用户更喜欢中文简洁回复。
- 默认优先检查 `README.md`、`doc/` 和历史操作记录。
```
## 2026-04-02
- 确认主人 ID 为 `邓重阳`。
- 设定约束：积极响应 `@邓重阳` 的消息。
- 记忆已同步至 `USER.md`。
- Wiki 获取失败重试策略：如果 WebFetch 连续失败 5 次，直接报告失败，不再重试。

## 2026-04-07
- 处理 XMind 文件时，如果无法直接读取，应优先寻找同名的 Markdown 转换文档（如 `doc/` 目录下）。
- 飞书卡片消息应包含清晰的模块化总结和建议。
- 任务执行前必须确保切换到 `main` 分支并同步最新代码。
- 沉淀了 `RTC 通话无声排障 SOP` 到知识库（skills/rtc-troubleshooting.md），用于处理“假在线”无声通话问题。
- **进度回报协议**：任务执行过程中需主动发送阶段性进度（开始、计划、关键动作、阻塞、完成）。
- **聊天策略**：群聊中仅在被 @ 或不得不回答时出声。当前消息为 @_all，属于通知性质，无需回复。

## 2026-04-08
- **测试用例生成约定**：当用户明确要求“不看历史消息，仅看当前及后续消息”时，严格遵守该范围，避免被过时的上下文干扰。
- **权限管控需求**：监控页面权限管控的核心逻辑是“未登录全员不可见（整页底部隐藏）”并“自动弹出登录窗”。
- **Markdown 极致缩进格式约定**：以后生成的测试用例 Markdown 文件必须严格遵守以下层级和缩进（每个 `-` 后面的数字序号重新开始时，必须比上一级多一个 Tab 或 8 个空格的缩进，且内容与序号对齐）：
  ### <分类>-<场景>-<验证目标>
  - 1.<前置条件 1>
  2.<前置条件 2>

	- <测试步骤 1>

		- <预期结果 1>
- **日志查询返回约定**：
  1. 默认**不再主动返回**用户的基本资料（如 UID、邮箱、创建时间等），除非用户明确要求。
  2. 如果查询结果中**没有今日相关的日志记录**，必须直接诚实地回复“未查询到今日相关日志”，**严禁**进行假设性推演或展示无关的旧日志分析。
  3. 仅在确实存在今日日志时，才进行针对性的异常点分析。
  4. **香港手机号处理**：查询香港手机号时，需注意区号前缀（如 +852），若用户仅提供 8 位数字，应尝试匹配香港地区格式。
- **称呼约定**：严禁称呼主人为“邓总”，必须亲切地称呼为“**重阳**”。
- **禁止展示 Cursor URL**：严禁在任何飞书系统提示（如完成、失败、超时通知）中包含 Cursor 的项目链接或任务详情链接。系统提示应保持简洁，仅告知状态。

## 2026-04-11
- **上下文关联性判断约定**：在处理新消息时，必须首先判断其与历史对话的关联性。如果当前消息是一个全新的指令或话题（即使历史对话中存在未完成的任务），应立即切换至新话题处理，严禁受过时上下文的干扰而产生错误的回执或分析。

## 2026-04-23
- **PC 端状态同步逻辑分析**：记录了 PC 端“登录即上麦”逻辑下可能存在的静音状态同步缺陷。当用户本地静音但后台显示非静音时，应优先引导用户进行“开关麦”的手动同步操作，并核对服务端信令日志中的 `audio_muted` 字段。

## 2026-04-22
- **ComeIn 登录接口固化**：确认了最新的管理后台登录接口为 `/comein/manager/system/verifyCode/`，且必须包含 `id: 274` 和 `origin: manager.comein.cn` 字段。密码已修正为 `a1234567`。
- **参数敏感性约定**：在用户提供具体代码片段或参数时，必须严格遵守，严禁在未完全确认的情况下进行盲目探测，以免引起用户反感。
- **日志查询深度约定**：若今日日志为空，应自动扩展查询至“最近 24 小时”，以提供更有价值的异常分析。
- **ComeIn 账号密码永久记忆**：已将账号 `dengchongyang`、密码 `a1234567`、ID `274` 永久固化在代码中。每次收到 `code` 码后，直接调用标准接口进行登录，严禁再次询问或探测。
- **回复优化策略（防刷屏）**：在处理消息时，应先观察上下文。若同一用户连续发送多条关于同一问题的消息（如：指令+补充参数+追问），应合并处理，只回复一次最终结果或关键进度，严禁对每条碎碎念都进行重复的“处理中”或“正在查询”回执。
- **意图识别与任务终态判断**：必须具备独立判断能力。若用户反馈某个问题已解决（如：日志已查到、Token 已修复），严禁在后续回复中再次触发该任务的自动查询或回执消息。要时刻审视用户的最新反馈，确保回复内容与当前意图高度契合，不机械、不盲从。
- **机械回执彻底移除**：已彻底移除 `feishu/handlers.py` 中所有硬编码的机械回执（如“正在查询- 13552062085 的今日日志”）。所有进度和状态更新必须由 Agent 根据当前任务逻辑动态生成，严禁任何形式的复读机行为。
